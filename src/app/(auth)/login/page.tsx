import type { Metadata } from "next";

export const metadata: Metadata = { title: "Ingresar" };

// Placeholder — Fase 1
export default function LoginPage() {
  return <div className="py-8 text-center text-muted-foreground">🔐 Login — Fase 1</div>;
}
