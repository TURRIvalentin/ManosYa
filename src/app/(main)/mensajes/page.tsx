import type { Metadata } from "next";

export const metadata: Metadata = { title: "Mensajes" };

// Placeholder — Fase 4
export default function MensajesPage() {
  return <div className="px-4 py-6">💬 Mensajes — Fase 4</div>;
}
