import type { Metadata } from "next";

export const metadata: Metadata = { title: "Mi perfil" };

// Placeholder — Fase 1
export default function PerfilPage() {
  return <div className="px-4 py-6">👤 Perfil — Fase 1</div>;
}
