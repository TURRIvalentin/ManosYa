import type { Metadata } from "next";

export const metadata: Metadata = { title: "Admin — ManosYa" };

// Placeholder — Fase 7
export default function AdminPage() {
  return <div className="p-4">🔧 Panel admin — Fase 7</div>;
}
